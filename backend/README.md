# Backend (planned)

This directory is reserved for the future HTTP API (for example FastAPI) that will expose graph queries and narrative features. Shared configuration and paths should import from the `chintu` package (`src/chintu/`) rather than duplicating path logic.
