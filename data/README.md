# Graph export CSVs (`data/chintu/`)

Sample **CHINTU** vertex/edge files for local development or tests. The HTTP API talks to **TigerGraph**; it does not read these CSVs at runtime.

Override the export directory with `CHINTU_EXPORT_DIR` in `.env` if needed.
